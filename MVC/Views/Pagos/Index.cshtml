       @model IEnumerable<MVC.Models.Pagos>

        @{
            ViewBag.Title = "Index";

        }


        <body>
            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.MedioPago)
                    </th>
                    <th></th>
                </tr>
    
            @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.MedioPago)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.Monto }) |
                        @Html.ActionLink("Details", "Details", new { id=item.Monto }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.Monto })
                    </td>
                </tr>
            }
    
            </table>
